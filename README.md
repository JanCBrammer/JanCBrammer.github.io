Derived from <https://github.com/ankitsultana/researcher>.

Serve locally by spinning up the devcontainer and running `jekyll serve`. See https://jekyllrb.com.

Build `resume.pdf` by running `cd resume && ./build_resume_pdf.sh`.
